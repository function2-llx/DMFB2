#include "math_models/Direction.h"

const Direction direction[5] = {Direction(0, 0), 
                                Direction(0, -1), 
                                Direction(0, 1), 
                                Direction(-1, 0), 
                                Direction(1, 0)}; 

const Direction zeroDirection = Direction(0, 0);            